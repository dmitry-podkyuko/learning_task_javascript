// Написать функцию которая будет принимать строку и возвращать объект 
// Пример работы функции:

convertToObject("abc") // { a: { b: { c: {} } } }


function convertToObject(str) {
  // тут ваш код 
  return
}